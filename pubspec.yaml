name: jogodavelha
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  drift: ^2.0.0
  drift_sqflite: ^2.0.0 # Se estiver usando SQLite

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.0.0
  drift_dev: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/kayn.png
    - assets/images/rhaast.png
    - assets/images/maxresdefault.jpg